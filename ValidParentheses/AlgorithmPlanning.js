/**
 * @param {string} s
 * @return {boolean}
 */
var isValid = function (s) {
    // solution 0
    // time: O(n)
    // space: O(n)
    // create map of closing brackets to open brackets
    // go through string
    // if open bracket
    // push onto stack
    // else
    // see if top of stack is corresponding opening bracket (via map)
    // if not, return false
    // return true
};