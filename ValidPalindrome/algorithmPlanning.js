/**
 * @param {string} s
 * @return {boolean}
 */
var isPalindrome = function (s) {
    // solution 0
    // time: O(n)
    // space: O(n)
    // lowercase all letters in the string
    // remove all non ASCII letter characters from the string
    // go through the string with two pointers, if the value in the string at each aren't the same, return false
    // return true

    // solution 0
    // time: O(n)
    // space: O(n)
    // lowercase all letters in the string
    // remove all non alpha numeric characters from the string
    // if string doesn't equal the reversed string, return false (else true)
};